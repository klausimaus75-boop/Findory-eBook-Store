import type { Metadata } from "next"; import Link from "next/link"; import { articles } from "@/data/articles"; export const metadata: Metadata = { title:"Ratgeber", description:"Verständliche Einordnungen zu digitaler Sicherheit und Vorsorge." }; export default function GuidePage() { return <section className="shell page-intro"><p className="category">Wissen im Alltag</p><h1>Ratgeber</h1><p className="lead">Klare Grundlagen, die helfen, Risiken einzuordnen und den nächsten Schritt ruhig zu gehen.</p><div className="article-grid article-grid-large">{articles.map(article => <article key={article.slug}><p className="category">{article.category}</p><h2><Link href={`/ratgeber/${article.slug}`}>{article.title}</Link></h2><p>{article.description}</p><Link className="text-link" href={`/ratgeber/${article.slug}`}>Artikel lesen →</Link></article>)}</div></section>; }
