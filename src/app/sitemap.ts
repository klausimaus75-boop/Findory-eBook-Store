import type { MetadataRoute } from "next"; import { ebooks } from "@/data/ebooks"; import { articles } from "@/data/articles"; import { canonicalUrl } from "@/config/site";
export const dynamic = "force-static";
export default function sitemap(): MetadataRoute.Sitemap { return ["","/ebooks","/ratgeber","/ueber-uns","/kontakt","/impressum","/datenschutz","/affiliate-hinweis","/themen/digitale-sicherheit","/themen/vorsorge","/themen/ki-und-selbststaendigkeit",...ebooks.map(x=>`/ebooks/${x.slug}`),...articles.map(x=>`/ratgeber/${x.slug}`)].map(url=>({url:canonicalUrl(url),lastModified:new Date()})); }
