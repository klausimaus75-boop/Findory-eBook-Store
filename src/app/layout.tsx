import type { Metadata } from "next"; import "./globals.css"; import { Header } from "@/components/header"; import { Footer } from "@/components/footer"; import { canonicalUrl, siteConfig } from "@/config/site";
export const metadata: Metadata = { metadataBase: new URL(canonicalUrl()), title: { default: `${siteConfig.name} | Praktische E-Books für Sicherheit und Vorsorge`, template: `%s | ${siteConfig.name}` }, description: "Entdecke verständliche E-Books über KI-Betrug, digitale Sicherheit, persönliche Vorsorge und moderne Selbstständigkeit.", alternates: { canonical: "/" }, openGraph: { type: "website", locale: "de_DE", siteName: siteConfig.name } };
export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) { return <html lang="de"><body><Header/><main>{children}</main><Footer/></body></html>; }
