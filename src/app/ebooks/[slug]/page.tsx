import type { Metadata } from "next"; import { notFound } from "next/navigation"; import Link from "next/link"; import { ebooks, getEbook } from "@/data/ebooks"; import { BookCover } from "@/components/book-cover"; import { AffiliateButton } from "@/components/affiliate-button"; import { Breadcrumbs } from "@/components/breadcrumbs";
export function generateStaticParams() { return ebooks.map(({ slug }) => ({ slug })); } export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const ebook = getEbook((await params).slug); return { title: ebook?.title, description: ebook?.shortDescription }; }
export default async function EbookPage({ params }: { params: Promise<{ slug: string }> }) { const ebook = getEbook((await params).slug); if (!ebook) notFound(); const similar = ebooks.filter(b => b.category === ebook.category && b.slug !== ebook.slug).slice(0, 3); const schema = { "@context": "https://schema.org", "@type": "Book", name: ebook.title, description: ebook.shortDescription, author: { "@type": "Person", name: "[AUTORENNAME ODER PSEUDONYM]" } }; return <section className="shell detail"><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema)}}/><Breadcrumbs items={[{label:"Startseite",href:"/"},{label:"E-Books",href:"/ebooks"},{label:ebook.title}]}/><div className="detail-hero"><BookCover ebook={ebook} priority/><div><p className="category">{ebook.category}</p><h1>{ebook.title}</h1><p className="lead">{ebook.subtitle}</p><p>{ebook.shortDescription}</p><span className="status">{ebook.status === "available" ? "Verfügbar" : "Demnächst"}</span>{ebook.status === "available" && <AffiliateButton href={ebook.amazonUrl}/>}</div></div><div className="detail-layout"><div><h2>Worum es geht</h2><p>{ebook.longDescription}</p><h2>Das hilft dir weiter</h2><ul className="check-list">{ebook.benefits.map(x => <li key={x}>{x}</li>)}</ul><h2>Für wen dieser Ratgeber gedacht ist</h2><ul>{ebook.targetAudience.map(x => <li key={x}>{x}</li>)}</ul></div><aside><h2>Inhaltsverzeichnis</h2>{ebook.chapters.map((chapter, i) => <div className="chapter" key={chapter.title}><span>{String(i + 1).padStart(2,"0")}</span><div><h3>{chapter.title}</h3><p>{chapter.description}</p></div></div>)}</aside></div><section className="preview-section"><h2>Blick ins Buch</h2><p>Die Leseprobe wird hier ergänzt, sobald das E-Book veröffentlicht ist.</p><div className="preview-pages"><div>Findory<br/><strong>Klare Schritte statt Unsicherheit.</strong></div><div>Checkliste<br/><strong>Was jetzt wichtig ist.</strong></div></div><h2>Bonusmaterial</h2><p>Je nach Thema ergänzt dieser Ratgeber Checklisten, übersichtliche Arbeitsblätter oder praktische Vorlagen.</p><h2>Häufige Fragen</h2><details><summary>Wann erscheint das E-Book?</summary><p>Der Status wird auf dieser Seite aktualisiert, sobald ein verbindlicher Veröffentlichungstermin feststeht.</p></details><details><summary>Wo kann ich das E-Book kaufen?</summary><p>Verfügbare E-Books verlinken deutlich gekennzeichnet zu Amazon.</p></details></section>{similar.length > 0 && <section className="section similar"><h2>Passende weitere Ratgeber</h2><div className="similar-links">{similar.map(book => <Link key={book.slug} href={`/ebooks/${book.slug}`}>{book.title} <span>→</span></Link>)}</div></section>}<p className="legal-notice">Dieses E-Book dient der allgemeinen Information und Prävention. Es ersetzt keine individuelle rechtliche Beratung, polizeiliche Beratung oder professionelle IT-Sicherheitsprüfung.</p></section>; }
