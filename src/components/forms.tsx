"use client"; import { FormEvent, useState } from "react";
export function NewsletterForm() { const [done, setDone] = useState(false); function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setDone(true); } return <form className="newsletter-form" onSubmit={submit}><label><span className="sr-only">Deine E-Mail-Adresse</span><input required type="email" placeholder="Deine E-Mail-Adresse" /></label><button className="button button-accent">Kostenlos eintragen</button>{done && <p role="status" className="form-success">Vielen Dank. Die Newsletter-Funktion wird in Kürze freigeschaltet.</p>}</form>; }
export function ContactForm() { const [done, setDone] = useState(false); function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setDone(true); } return <form className="contact-form" onSubmit={submit}><label>Name<input required name="name" autoComplete="name" /></label><label>E-Mail-Adresse<input required type="email" name="email" autoComplete="email" /></label><label>Betreff<input required name="subject" /></label><label>Nachricht<textarea required name="message" rows={6}/></label><p className="form-help">Diese lokale Formularfunktion überträgt aktuell keine Daten.</p><button className="button button-primary">Nachricht prüfen</button>{done && <p role="status" className="form-success">Vielen Dank. Deine Nachricht wurde lokal geprüft. Die Kontaktfunktion wird in Kürze freigeschaltet.</p>}</form>; }
