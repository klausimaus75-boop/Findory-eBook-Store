import Link from "next/link"; import type { Ebook } from "@/data/ebooks"; import { BookCover } from "./book-cover";
export function EbookCard({ ebook }: { ebook: Ebook }) { return <article className="ebook-card"><BookCover ebook={ebook}/><div className="card-copy"><p className="category">{ebook.category}</p><h3><Link href={`/ebooks/${ebook.slug}`}>{ebook.title}</Link></h3><p className="subtitle">{ebook.subtitle}</p><p>{ebook.shortDescription}</p><div className="card-bottom"><span className="status">{ebook.status === "coming-soon" ? "Demnächst" : "Verfügbar"}</span><Link className="text-link" href={`/ebooks/${ebook.slug}`}>Mehr erfahren <span aria-hidden>→</span></Link></div></div></article>; }
